# 项目记忆：ThreadX_MQTT_H750_Template_clean

## 当前状态

- 项目路径：`C:\Users\DJ\Desktop\ThreadX_MQTT_H750_Template_clean`
- 当前 Git 分支：`WiFi_MQTT`
- 最近提交：`13a4465 feat: switch to ESP8266-only MQTT path, add USART3 logging`
- 当前工作区存在未提交源码改动：`App/esp8266/esp8266_config.h`、`Core/Inc/main.h`、`Core/Src/app_threadx.c`、`Core/Src/gpio.c`、`Core/Src/main.c`、`Core/Src/stm32h7xx_it.c`、`Core/Src/usart.c`。
- 工程已从偏 NetX Duo Ethernet/MQTT 的模板，切到以 ESP8266 ESP-AT 为主的 WiFi/MQTT 路径。
- `Core/Src/app_threadx.c` 中 `WIFI_SERVICE_AUTOSTART` 当前为 `0`，因此默认只启动 blink 线程，不自动启动 ESP8266 WiFi/MQTT 服务。

## 已确认架构

- 启动链路：`main()` -> HAL/时钟/外设初始化 -> `MX_ThreadX_Init()` -> `tx_kernel_enter()` -> `App_ThreadX_Init()` 创建应用线程。
- 基础线程：`blink_thread` 周期翻转 PB0 `KEEP_Pin`，并递增 `blink_thread_count`。
- WiFi/MQTT 服务入口：`esp8266_service_init()`，被 `wifi_service_init()` 包装；是否创建由 `WIFI_SERVICE_AUTOSTART` 控制。
- ESP8266 服务会创建三个线程：RX 解析线程、MQTT 业务状态机线程、heartbeat 线程。
- USART1 使用 DMA circular RX 和 DMA TX 对接 ESP8266；相关 ISR 回调在 `Core/Src/stm32h7xx_it.c` 中转发。
- USART3 被 `_write()` 用作 `printf` 日志输出。
- MQTT AT 封装位于 `App/esp8266/esp8266_mqtt.c`，当前包含 connect、publish、subscribe、reconnect 等基础能力。
- PC 侧工具包括静态 Web 控制页和 MQTT cloud/local bridge。

## 关键决策

- 当前优先走 ESP8266-only MQTT 路径，而不是直接启用 STM32 Ethernet + NetX Duo MQTT 路径。
- NetX Duo、FileX、中间件源码仍保留在工程中，作为后续扩展基础；但保留源码不代表应用层已经启用。
- 敏感信息不写入项目记忆；如需记录，只记录“配置所在文件/环境变量”，不记录具体密码。
- 对 CubeMX 生成文件的人工修改必须尽量放在 `USER CODE` 区域，避免重新生成时丢失。

## 已知风险

- `App/esp8266/esp8266_config.h` 当前包含本地 WiFi/MQTT 配置，后续提交或共享前必须脱敏或改为本地私有配置方案。
- `esp8266.c` 中 DMA RX 缓冲会执行 DCache invalidate；需要确认 DCache 实际状态，避免在 DCache 未启用时仍执行 cache 维护导致 HardFault。
- 项目中已有关于 STM32H750 + ThreadX + NetX Duo LED 卡死/HardFault 的分析文档，核心风险与 ETH driver、DCache、DMA 缓冲有关。
- 根目录 `ENGINEERING_AUDIT_REPORT.md` 和 `STM32H750 + ThreadX + NetX Duo LED 卡死问题分析.md` 在当前终端显示为乱码，可能是编码不一致；阅读前应注意转换编码或用支持原编码的编辑器打开。
- Web/bridge 文档中仍有云端、本地 Mosquitto、NetX Duo 等历史/并行路径说明，和当前 ESP8266-only 固件路径可能不完全一致。

## 重要文件索引

- `ThreadX_MQTT_H750_Template.ioc`：CubeMX 配置。
- `MDK-ARM/ThreadX_MQTT_H750_Template.uvprojx`：Keil 工程。
- `Core/Src/main.c`：主初始化与 `_write()` 日志重定向。
- `Core/Src/app_threadx.c`：ThreadX 应用线程入口。
- `Core/Src/usart.c`：USART1/USART3 与 DMA 配置。
- `Core/Src/stm32h7xx_it.c`：USART1 与 DMA ISR 回调转发。
- `App/esp8266/`：ESP8266 AT、MQTT、RingBuffer、Parser、配置。
- `web/led-control.html`：浏览器控制页面。
- `tools/mqtt-cloud-bridge.mjs`：MQTT 桥接工具。
- `package.json`：PC 工具依赖与脚本，`npm run bridge` 可启动桥接。

## 待办事项

- 明确是否要把 `WIFI_SERVICE_AUTOSTART` 改为 `1` 作为默认启动模式。
- 检查 ESP8266 RX DMA 的 DCache 维护逻辑，确认是否需要加 `SCB->CCR & SCB_CCR_DC_Msk` 判断。
- 将 WiFi/MQTT 真实凭据从可提交头文件中抽离，改成本地私有头文件、构建宏或其他不入库配置。
- 统一当前固件 MQTT topic 与 `web/led-control.html` / `tools/mqtt-cloud-bridge.mjs` 使用的 topic 前缀。
- 在 Keil 中做一次 Rebuild，并记录编译结果、下载结果和串口日志。
- 确认 PB1 `LED_CTRL_Pin` 的 MQTT 命令处理是否已经实现；如果尚未实现，补齐 topic payload 到 GPIO 控制的映射。

## 变更记录

### 2026-05-27

- 为工程根目录准备 `agent.md` 和 `项目记忆.md`。
- 记录当前工程以 ESP8266 ESP-AT + MQTT 为主路径，ThreadX 作为应用调度基础。
- 记录当前分支、最近提交、未提交改动、关键入口和下一步风险点。

## 给后续 Agent 的提示

- 先读本文件和 `agent.md`，再看源码。
- 不要把根目录已有乱码审计文档直接复制进新文档；需要时先处理编码。
- 如果修改了启动路径、线程创建、MQTT topic、WiFi/MQTT 配置或硬件引脚，请同步更新本文件。
- 如果完成硬件验证，请记录串口日志现象、Keil 编译结果、下载方式和板端 LED/MQTT 行为。

