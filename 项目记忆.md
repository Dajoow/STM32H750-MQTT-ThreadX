# 项目记忆：ThreadX_MQTT_H750_Template_clean

## 当前状态

- 项目路径：`C:\Users\DJ\Desktop\ThreadX_MQTT_H750_Template_clean`
- 当前主要分支：`WiFi_MQTT`
- 当前目标：STM32H750 通过 ESP8266 ESP-AT 连接 WiFi 和 HiveMQ Cloud，周期上传 DHT11 温湿度数据，并预留 Web/App MQTT 接口。
- 当前已跑通链路：`DHT11 -> STM32H750 -> USART1 -> ESP8266 -> WiFi/MQTT TLS -> HiveMQ Cloud -> Web Client`。
- HiveMQ Web Client 已看到有效数据，例如：`temperature=26.00/27.00`、`humidity=57.00~89.00`、`raw=0`、`valid=1`。
- 当前不使用 STM32 自己跑 NetX TCP/IP/TLS 做云 MQTT，ESP8266 负责 WiFi、TCP/TLS、MQTT，STM32 负责业务、AT 控制、传感器和 JSON 数据。

## 当前模块结构

- `App/WIFI_8266/`：ESP8266 WiFi MQTT 业务模块、配置、传感器数据适配。
- `App/WIFI_8266/wifi_8266.c`：WiFi/MQTT 主流程，使用 ESP-AT MQTT 指令，telemetry 使用 `AT+MQTTPUBRAW` 发布 JSON。
- `App/WIFI_8266/wifi_8266_config.h`：非敏感配置、topic、周期、调试开关；真实 WiFi/MQTT 密码不应写在这里。
- `App/WIFI_8266/wifi_8266_secrets.h`：本地真实 WiFi/MQTT 凭据，应被 `.gitignore` 忽略，不提交。
- `App/WIFI_8266/wifi_8266_secrets.example.h`：可提交的示例凭据模板。
- `App/WIFI_8266/sensor_data.c`：传感器数据统一入口，当前接入 DHT11，输出给 MQTT telemetry。
- `Drivers/BSP/Components/DHT11/`：DHT11 单总线驱动。
- `Drivers/BSP/Components/PCF8574/`：PCF8574 IO 扩展驱动。
- `Drivers/BSP/Components/Delay/`：DWT 微秒/毫秒延时，DHT11 时序依赖它。
- `Core/Src/main.c`：HAL 外设初始化、DWT 初始化、PCF8574 初始化、进入 ThreadX。
- `Core/Src/app_threadx.c`：ThreadX 线程创建入口，应只放线程创建/启动相关代码，不堆业务实现。
- `Core/Src/usart.c`：USART1 给 ESP8266，USART3 给调试日志。

## MQTT 约定

- MQTT Broker：HiveMQ Cloud。
- MQTT TLS：ESP8266 AT 侧处理，配置中 `WIFI8266_MQTT_SCHEME` 使用 `"2"`。
- MQTT 端口：`8883`。
- Client ID：`stm32h750_esp8266_001`。
- telemetry topic：`stm32h750/esp8266_001/telemetry`。
- status topic：`stm32h750/esp8266_001/status`。
- command topic：`stm32h750/esp8266_001/command`。
- Web/App 后续只需要订阅 telemetry/status，向 command 发布控制消息即可。

## 串口分工

- USART1：STM32 与 ESP8266 通信，发送 ESP-AT 指令。
- USART3：调试日志输出到 PC 串口助手。
- 不要把 ESP8266 和 printf 混在同一个串口上，否则 AT 通信和日志会互相污染。
- 之前 `printf` 曾导致系统卡住，后续优先使用已验证的 USART3 raw transmit/log 封装，避免在中断或关键时序里调用阻塞 printf。

## 传感器链路

- 当前传感器：DHT11。
- 硬件接口：TEMP&HUMI SENSOR 四脚座，`GND / NC / DQ / VCC`。
- DQ 网络：`1WIRE_DQ`。
- MCU 引脚：`PG10`，代码宏为 `DTH11_Pin` / `DTH11_GPIO_Port`。
- DHT11 读取成功时 MQTT JSON：
  - `temperature_c_x100 = temperature * 100`
  - `humidity_rh_x100 = humidity * 100`
  - `raw = 0`
  - `valid = 1`
- DHT11 读取失败时 MQTT JSON：
  - `temperature = 0.00`
  - `humidity = 0.00`
  - `raw/debug = dht11_status`
  - `valid = 0`

## 关键初始化顺序

必须保持这个顺序：

```c
MX_GPIO_Init();
MX_I2C1_Init();
MX_I2C2_Init();
MX_USART1_UART_Init();
MX_USART3_UART_Init();

DWT_Init();

/* PG10 is shared by 1WIRE_DQ and PCF8574 IIC_INT.
 * Initialize PCF8574 and read one IO to release/clear the INT line before DHT11 access.
 */
pcf8574_init();
pcf8574_read_bit(BEEP_IO);

MX_ThreadX_Init();
```

原因：DHT11 单总线时序依赖 DWT；同时 PG10 和 PCF8574 INT 复用，如果不先初始化/读取 PCF8574，PG10 可能被 INT 拉低，DHT11 永远读不到高电平。

## 重大踩坑记录：PG10 同时连接 1WIRE_DQ 和 PCF8574 IIC_INT

### 现象

- DHT11 上传一直失败，HiveMQ 上显示：
  - `temperature=0.00`
  - `humidity=0.00`
  - `raw=50000`
  - `valid=0`
- DQ 自检显示 `raw=50000`。

### 自检码含义

DQ 自检编码：

```text
raw = 5eabc
```

字段含义：

```text
e = 临时切成 GPIO_INPUT + PULLUP 后读到的电平
a = 开漏释放 2 tick 后读到的电平
b = MCU 主动拉低 100us 后读到的电平
c = 开漏释放 100us 后读到的电平
d = 开漏释放 1ms 后读到的电平
```

典型判断：

```text
61011：硬件线正常，MCU 能拉低，也能释放回高
50000：输入上拉仍为低，DQ 被外部拉低或短路
60000：输入上拉能拉高，但开漏释放不高，GPIO 输出配置/初始化可疑
61111：MCU 拉不低，可能不是 PG10 或线没接到 DQ
```

### 根因

原理图中 PG10 同时作为两个信号：

```text
PG10 = 1WIRE_DQ
PG10 = IIC_INT / PCF8574_INT
```

PCF8574 的 INT 是中断输出，未初始化或未读取/清除状态时可能把 PG10 拉低，导致 DHT11 DQ 线一直低电平。

### 解决办法

在进入 ThreadX、DHT11 初始化/读取前，先执行：

```c
pcf8574_init();
pcf8574_read_bit(BEEP_IO);
```

这会初始化 PCF8574，并通过读一次 IO 释放/清除 INT 状态。之后 DHT11 正常读取。

### 后续注意

- 不要随意删除 `main.c` 中 PCF8574 初始化和 `pcf8574_read_bit(BEEP_IO)`。
- 如果未来 DHT11 又变成 `raw=50000`，先查 PG10 是否再次被 PCF8574 INT 或其他外设拉低。
- 如果重新生成 CubeMX 代码，确认 PG10 的用户代码和 PCF8574 初始化没有丢。
- 硬件上这属于复用风险，同一根线上挂 DHT11 单总线和 PCF8574 INT，软件初始化顺序必须配合。

## DQ 自检开关

文件：`App/WIFI_8266/wifi_8266_config.h`

```c
#define SENSOR_DQ_SELF_TEST_ENABLE 0U
```

- 正常业务运行必须为 `0U`。
- 排查 DHT11/DQ 硬件问题时可以临时改为 `1U`。
- 自检开启后不会读取真实 DHT11，只上传 DQ 电平码，`valid=0` 是预期现象。

## DHT11 模块脚序风险

板载接口是：

```text
GND / NC / DQ / VCC
```

很多 DHT11 模块常见脚序可能是：

```text
VCC / DATA / NC / GND
```

如果模块脚序不匹配，可能出现：

- DQ 被拉低。
- VCC/GND 接反。
- 传感器无响应。
- 自检 `raw=50000`。

硬件异常时先拔掉 DHT11 模块，再看 DQ 自检；空载应能回高。

## ESP8266 与 MQTT 已验证现象

- ESP8266 AT 固件支持 MQTT 指令。
- 已验证 AT 指令包括：
  - `AT`
  - `ATE0`
  - `AT+CWMODE=1`
  - `AT+CWJAP`
  - `AT+MQTTUSERCFG`
  - `AT+MQTTCONN`
  - `AT+MQTTSUB`
  - `AT+MQTTPUBRAW`
- HiveMQ Web Client 可订阅 `stm32h750/esp8266_001/#` 查看 telemetry/status。
- 发布 JSON 长消息时优先使用 `AT+MQTTPUBRAW`，不要用普通 `AT+MQTTPUB` 硬塞复杂 JSON，否则容易因为引号/转义/长度导致 `ERROR`。

## 容易出错的 Bug 清单

### 1. PG10 被 PCF8574 INT 拉低

- 现象：DHT11 失败，DQ 自检 `raw=50000`。
- 原因：PG10 同时是 `1WIRE_DQ` 和 `IIC_INT`。
- 修复：DHT11 前执行 `pcf8574_init(); pcf8574_read_bit(BEEP_IO);`。

### 2. DHT11 自检开关忘记关

- 现象：HiveMQ 一直显示 `valid=0`，raw 是 5xxxx/6xxxx 诊断码。
- 修复：`SENSOR_DQ_SELF_TEST_ENABLE` 改回 `0U`。

### 3. DWT 未初始化导致 DHT11 时序失败

- 现象：DHT11 数据不稳定或一直失败。
- 原因：DHT11 驱动使用 `DWT_Delay_us/ms`。
- 修复：进入 ThreadX 前调用 `DWT_Init()`。

### 4. DHT11 模块脚序不匹配

- 现象：DQ 一直低、无响应、甚至模块发热。
- 修复：核对模块实际 `VCC/DATA/NC/GND` 与板载 `GND/NC/DQ/VCC` 是否一致。

### 5. 普通 `AT+MQTTPUB` 发布 JSON 失败

- 现象：MQTT 已连接，但发布 JSON 返回 `ERROR`。
- 原因：普通 `MQTTPUB` 对引号、转义、长度更敏感。
- 修复：使用 `AT+MQTTPUBRAW`。

### 6. 真实密码误提交

- 现象：Git diff/status 中出现真实 WiFi 或 HiveMQ 密码。
- 修复：真实凭据只放 `wifi_8266_secrets.h`，并确保 `.gitignore` 忽略；提交前检查 `git diff --cached`。

### 7. USART1/USART3 混用

- 现象：ESP8266 AT 返回混乱，或者串口助手看不到日志。
- 原因：USART1 应只给 ESP8266，USART3 应只给调试输出。
- 修复：硬件和代码都保持这个分工。

### 8. printf/日志阻塞影响 ThreadX

- 现象：LED 线程不闪、系统卡住、串口无输出。
- 经验：优先使用已验证的 USART3 原始发送日志方式；避免在中断、DHT11 微秒时序、AT parser 临界区里大量阻塞打印。

### 9. CubeMX 重新生成覆盖用户代码

- 现象：PCF8574 初始化、DWT 初始化、USART3 日志、DHT11 引脚配置丢失。
- 修复：重新生成后检查 `Core/Src/main.c`、`Core/Src/gpio.c`、`Core/Src/usart.c`、`MDK-ARM/ThreadX_MQTT_H750_Template.uvprojx`。

## 验证流程

1. 编译 Keil 工程，确认 Rebuild 无 error。
2. 下载固件。
3. 串口助手打开 USART3，确认有 ESP8266 / MQTT 日志。
4. HiveMQ Web Client 订阅：`stm32h750/esp8266_001/#`。
5. 确认收到 status：`state=online`。
6. 确认 telemetry：`valid=1`、`raw=0`、温湿度非 0。
7. 如果 telemetry 异常：先打开 `SENSOR_DQ_SELF_TEST_ENABLE` 看 raw 诊断码。

## 后续扩展接口

- Web 端：订阅 `telemetry/status`，向 `command` 发布控制 JSON。
- App 端：同 Web 端 topic，建议统一 payload 格式。
- 后续可扩展：继电器、蜂鸣器、LED 控制、HomeAssistant、EMQX、OneNET、阿里云。
- 建议 command payload 预留：

```json
{"target":"led","value":1}
```

或：

```json
{"target":"beep","value":0}
```

## 给后续 Agent 的提示

- 先读本文件，再看源码。
- 不要把 `wifi_8266_secrets.h` 或真实密码写入提交/文档。
- 修改 DHT11 前先确认 PG10 与 PCF8574 INT 的复用关系。
- 如果 DHT11 异常，优先用 DQ 自检码定位硬件电平，再谈时序。
- 如果 MQTT 发布 JSON 异常，优先检查是否仍使用 `AT+MQTTPUBRAW`。
- 如果重新整理架构，保持 `app_threadx.c` 只负责线程创建，业务实现留在 `App/WIFI_8266/`。

## 2026-06-01 更新：Web POST 配网与 MQTT reconnect 状态机

### 当前稳定链路

- STM32H750 通过 USART1 控制 ESP8266 AT 固件，USART3 只输出调试日志。
- 上电后进入 ESP8266 SoftAP 配网：AP 为 `STM32_WIFI_SETUP`，浏览器访问 `http://192.168.4.1`。
- 配网页面使用 POST `/set`，body 格式为 `ssid=...&pass=...`。
- 保留 GET `/set?ssid=...&pass=...` 只作为浏览器旧缓存兼容，不作为推荐接口。
- WiFi 密码和 HiveMQ 密码不要从 USART3 日志打印出来。

### HiveMQ 凭据使用规则

- 设备端使用独立 Credentials，例如 `stm32_h750_device`。
- Web Client 使用独立 Credentials，例如 `stm32_h750_web`。
- App 建议使用独立 Credentials，例如 `stm32_h750_app`。
- 不要让设备、网页、App 共用同一个 MQTT Client ID 或同一套账号同时在线。
- 典型故障：设备反复出现 `+MQTTDISCONNECTED:0`、`AT+MQTTCONN` 返回 `ERROR`，但 WiFi 正常 `WIFI GOT IP`。优先检查 HiveMQ Web Client 是否占用了设备凭据或 Client ID。

### 正式 reconnect 状态机

- `PROVISION`：启动 SoftAP + HTTP Server，等待网页输入 WiFi SSID/密码。
- `WIFI_CONNECT`：切换 STA 模式，执行 `AT+CWJAP`，成功后进入 MQTT 连接。
- `MQTT_CONNECT`：执行 `AT+CIPMUX=0`、链路检查、`AT+MQTTCLEAN=0`、`AT+MQTTUSERCFG`、`AT+MQTTCONN`。
- `MQTT_SUBSCRIBE`：订阅 `command` 和 `nodes/+/command`。
- `RUN`：周期读取节点数据，只发布聚合 telemetry 包。
- `RESET`：连续 MQTT 失败后只软复位 ESP8266 AT 栈，再重新连接 WiFi，不复位 STM32。

### 恢复策略

- WiFi 失败：回到 `PROVISION`，重新打开网页配网。
- MQTT 失败：先只重试 MQTT，不立即重连 WiFi。
- MQTT 连续失败达到阈值：进入 `RESET`，执行 `AT+RST` 后重新走 WiFi/MQTT。
- 发布失败：认为 MQTT 链路异常，回到 `MQTT_CONNECT`。
- 运行态发现 WiFi 断开：回到 `WIFI_CONNECT`。
- 运行态发现 MQTT 断开：回到 `MQTT_CONNECT`。

### 当前 MQTT 上报约定

- 只保留一个周期上报包，Topic：`stm32h750/esp8266_001/nodes/telemetry`。
- 不再重复发送旧的 `status`、`availability`、`meta/nodes`、单节点 telemetry。
- 聚合包格式示例：

```json
{"deviceId":"stm32h750_esp8266_001","seq":1,"tick":61762,"nodes":[{"nodeId":"room_temp_humi","type":"dht11","Temp":25.00,"Hum":56.00,"value":0,"valid":1},{"nodeId":"living_temp_humi","type":"temp_humi","Temp":0.00,"Hum":0.00,"value":0,"valid":0},{"nodeId":"room_light","type":"light","Temp":0.00,"Hum":0.00,"value":0,"valid":0},{"nodeId":"living_light","type":"light","Temp":0.00,"Hum":0.00,"value":0,"valid":0}]}
```

### 本轮新增踩坑

- HTTP 请求 buffer 不要放在线程函数局部栈中；2048 字节局部数组会挤爆 ThreadX 小栈，表现为网页打不开、服务反复重启、只能看到 `AT sync try`。
- 配网页面改 POST 后，响应头需要带 `Content-Length` 和 `Connection: close`，避免浏览器等待或误判连接被拒绝。
- 从 SoftAP 配网切回 STA 后要关闭 HTTP Server，并恢复 `AT+CIPMUX=0`，否则后续 MQTT 连接可能被旧 TCP server/mux 状态干扰。
- `AT+MQTTUSERCFG` 日志必须隐藏命令正文，避免 HiveMQ 密码出现在串口助手和提交记录中。
- 如果 `AT+CWJAP?` 正常、`AT+CIPSTATUS` 为 `STATUS:2`，但 MQTT 仍失败，优先检查 HiveMQ 凭据/Client ID 冲突，而不是先怀疑 WiFi。
